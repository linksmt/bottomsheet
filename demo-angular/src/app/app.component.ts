import { Component, NgModule } from "@angular/core";

@NgModule({
    imports: [ ],
})

@Component({
    moduleId: module.id,
    selector: "ns-app",
    templateUrl: "app.component.html"
})
export class AppComponent { }
